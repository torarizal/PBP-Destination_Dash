import 'package:flutter/material.dart';

class ListReligi extends StatelessWidget {
  const ListReligi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Destinasi Religi')),
      body: const Center(
        child: Text('Ini adalah halaman untuk Destinasi Religi.'),
      ),
    );
  }
}
